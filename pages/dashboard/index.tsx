import DashboardHome from "../../components/dashboard/home";
function Dashboard() {
	return (
		<>
			<DashboardHome user={{}} summary={{}} projectsFunded={{}} updates={{}} />
		</>
	);
}

export default Dashboard;
